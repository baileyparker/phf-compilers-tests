PROGRAM max;

VAR x, y: INTEGER;
CONST q = 1231;

PROCEDURE max(a: q; b: y): INTEGER;
    VAR r: INTEGER;
BEGIN
    IF a < b THEN
        r := b
    ELSE
        r := a
    END
RETURN r
END max;

BEGIN
  READ x;
  READ y;
  WRITE max(x, y)
END max.

